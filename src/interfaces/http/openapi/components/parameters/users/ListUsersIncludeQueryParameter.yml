components:
  parameters:
    ListUsersIncludeQueryParameter:
      name: include
      in: query
      description: Association to include with each user
      required: false
      allowReserved: true
      schema:
        type: array
        items:
          type: string
          enum:
            - posts
            - comments
      style: form
      explode: false
