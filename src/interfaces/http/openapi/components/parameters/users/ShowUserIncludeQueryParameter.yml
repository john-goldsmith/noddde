components:
  parameters:
    ShowUserIncludeQueryParameter:
      name: include
      in: query
      description: Association to include with the user
      required: false
      allowReserved: true
      schema:
        type: array
        items:
          type: string
          enum:
            - posts
            - comments
      style: form
      explode: false
