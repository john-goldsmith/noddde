components:
  schemas:
    ReplaceUserRequestSchema:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/ReplaceUserRequestData'
      additionalProperties: false
