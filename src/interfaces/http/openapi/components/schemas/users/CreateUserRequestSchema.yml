components:
  schemas:
    CreateUserRequestSchema:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/CreateUserRequestData'
      additionalProperties: false
