components:
  schemas:
    UpdateUserRequestSchema:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/UpdateUserRequestData'
      additionalProperties: false
