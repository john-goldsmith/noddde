components:
  schemas:
    ServerError:
      type: object
      properties:
        errors:
          type: array
          items:
            $ref: '#/components/schemas/ServerErrorItem'
      additionalProperties: false
