components:
  responses:
    ListHealthOkResponse:
      description: OK
      content:
        application/vnd.api+json:
          schema:
            $ref: '#/components/schemas/ListHealthResponseSchema'
